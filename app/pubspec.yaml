name: app
description: "Binary Inspector desktop reference application."
publish_to: 'none'

version: 1.10.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  desktop_drop: ^0.5.0
  file_picker: ^8.1.0
  binary_inspector_sdk:
    path: ../sdk

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/app-icon.png
